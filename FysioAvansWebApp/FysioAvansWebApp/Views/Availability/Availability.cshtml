@model IEnumerable<AvailabilityDetailsViewModel>
@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-lg-12">
        @Html.ActionLink("Plan afspraak", "Create", "PTreatment", null, new { @class = "btn btn-success color-green text-dark" })
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <td>Id</td>
                    <td>Beschrijving</td>
                    <td>Behandeldatum</td>
                    <td>Fysiotherapeut</td>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() > 0)
                {
                    @foreach (var Availability in Model)
                    {
                        <tr>
                            <td>@Availability.AvailabilityId</td>
                            <td>@Availability.StartAvailability</td>
                            <td>@Availability.EndAvailability</td>    
                            <td>@Availability.PhysioFirstname @Availability.PhysioLastname</td>
                        </tr>
                    }
                }
                else
                {
                <td colspan="5" class="text-center">
                    No records found
                </td>
            }
                </tbody>
            </table>
        </div>
    </div>

